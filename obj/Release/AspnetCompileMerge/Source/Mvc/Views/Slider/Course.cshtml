@model  SitefinityWebApp.Mvc.Models.SliderModel

<section class="@Model.ContainerClasses">
	@foreach (var item in Model.DynamicContentItems)
	{
		<div class="@Model.TileClasses">
			@{ Html.RenderPartial("_SliderTileCourse", item); }
		</div>
	}
</section>
